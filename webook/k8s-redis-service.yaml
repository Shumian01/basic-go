apiVersion: v1
kind: Service
metadata:
  name: webook-redis
spec:
  selector:
    app: webook-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
      #外部访问的端口 必须是30000-32767
      nodePort: 30003
  type: NodePort

  